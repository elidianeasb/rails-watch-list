<%= link_to list_path(list) do %>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= image_path list.picture_url %>)">
      <%= list.name %>
  </div>
<% end %>
